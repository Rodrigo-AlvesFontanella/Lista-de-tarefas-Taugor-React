class Tarefa {
    constructor(id, firstname, atividade, status, descricao, local, date, ){
        this.id = id;
        this.firstname = firstname;
        this.atividade = atividade;
        this.status = status;
        this.descricao = descricao;
        this.local = local;
        this.date = date;
    
    }
}

export default Tarefa;